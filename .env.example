# Copy to .env and adjust before running.

NODE_ENV=production

# PostgreSQL
PGUSER=amit
PGPASSWORD=replace_with_strong_password
PGDATABASE=amit
PG_PORT=5432

# Backend
BACKEND_PORT=3000
ADMIN_KEY=replace_with_random_long_string
CORS_ORIGIN=http://localhost:8080

# Frontend
FRONTEND_PORT=8080
